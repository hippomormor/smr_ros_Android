<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.app.org.MainActivity"
    android:background="#e8e8e8">

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Cam"
        android:id="@+id/camButton"
        android:elevation="50dp"
        android:textStyle="bold"
        android:layout_gravity="bottom"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Map"
        android:id="@+id/mapButton"
        android:elevation="50dp"
        android:textStyle="bold"
        android:layout_gravity="bottom"
        android:layout_alignParentBottom="true"
        android:layout_alignParentStart="true" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Quit"
        android:id="@+id/quitButton"
        android:textStyle="bold"
        android:layout_gravity="bottom"
        android:layout_alignParentBottom="true"
        android:layout_alignParentEnd="true" />

    <ImageView
        android:id="@+id/imageViewMenu"
        android:src="@drawable/smr"
        android:layout_width="match_parent"
        android:layout_height="275dp"
        android:layout_above="@+id/camButton"
        android:layout_alignParentStart="true"
        android:layout_marginBottom="31dp" />

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:srcCompat="@drawable/dtu_logo"
        android:id="@+id/imageView"
        android:layout_toStartOf="@+id/camButton"
        android:layout_above="@+id/mainmenuTitle" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textAppearance="?android:attr/textAppearanceLarge"
        android:text="@string/app_name"
        android:id="@+id/mainmenuTitle"
        android:textStyle="bold|italic"
        android:textColor="#000000"
        android:textSize="60sp"
        android:textAlignment="center"
        android:layout_above="@+id/imageViewMenu"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="13dp" />

</RelativeLayout>
